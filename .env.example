# ── Gmail Setup (RECOMMENDED) ──────────────────────────────────────────────
# 1. Go to Google Account → Security → 2-Step Verification (enable it)
# 2. Then: Google Account → Security → App Passwords
# 3. Create an App Password for "Mail" → copy the 16-char password
GMAIL_USER=patelsandesh1@gmail.com
GMAIL_APP_PASSWORD=buwc rvfr hwuc unvq

# ── Server ─────────────────────────────────────────────────────────────────
PORT=5000

# ── CORS (comma-separated, or * for all) ──────────────────────────────────
ALLOWED_ORIGINS=https://sandeshpatel1.github.io,http://localhost:5173

# ── Alternative SMTP (Brevo / Mailgun / etc.) ─────────────────────────────
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_pass
